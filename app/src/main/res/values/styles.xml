<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
    </style>

    <style name="Toolbar" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/blue</item>
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="BottomNavigationView">
        <item name="itemIconTint">@drawable/bottom_navigation_item_selector</item>
        <item name="itemTextColor">@drawable/bottom_navigation_item_selector</item>
    </style>

    <style name="BottomNavigation.itemTextAppearance" parent="@style/TextAppearance.AppCompat.Caption">
        <item name="android:textSize">@dimen/text_size_xsmall</item>
    </style>

    <style name="TextView" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/text_size_normal</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/textColorPrimary</item>
    </style>

    <style name="TextView.Title" parent="TextView">
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textSize">@dimen/text_size_xl</item>
    </style>

    <style name="TextView.Artist"></style>

    <style name="TextView.Price" parent="TextView">
        <item name="android:textColor">@color/textColorOrange</item>
        <item name="android:textSize">@dimen/text_size_price</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextView.TimeStamp" parent="TextView">
        <item name="android:textSize">@dimen/text_size_small</item>
    </style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/default_stroke_weight</item>
        <item name="android:background">@color/strokeColor</item>
    </style>

</resources>
